name: travel_in
description: Travel In - Yemen luxury transport booking
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  google_sign_in: ^6.2.1

flutter:
  uses-material-design: true
